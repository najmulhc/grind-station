#include <iostream>
#include <vector>
#include <unordered_set>
using namespace std;

int main()
{
    vector<int> nums = {1, 0, 1, 2};
    unordered_set<int> hash; 
    for(int num: nums) {
        hash.insert(num);
    }

    if(hash[34] ==)
    return 0;
}